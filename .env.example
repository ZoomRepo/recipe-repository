# Database configuration
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=reciperepository
DB_PASSWORD=changeme
DB_NAME=reciperepository
DB_POOL_NAME=recipe_web_pool
DB_POOL_SIZE=5

# Flask application configuration
PAGE_SIZE=20
SECRET_KEY=replace-me

# Login gate configuration
LOGIN_GATE_ENABLED=false
LOGIN_GATE_CODE_TTL_MINUTES=10
LOGIN_GATE_SESSION_LIFETIME_MINUTES=720

# Mail configuration
MAIL_ENABLED=false
MAIL_HOST=localhost
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_USE_TLS=true
MAIL_SENDER=no-reply@reciperepository.local

# Elasticsearch configuration
ELASTICSEARCH_URL=http://localhost:9200
ELASTICSEARCH_USERNAME=elastic
ELASTICSEARCH_PASSWORD=
ELASTICSEARCH_RECIPE_INDEX=recipes
ELASTICSEARCH_SCRAPER_INDEX=scraper-events
ELASTICSEARCH_TIMEOUT=10
